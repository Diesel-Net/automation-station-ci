proxmox_vm_id: 130
proxmox_vm_name: unity
proxmox_realm: pve
proxmox_user: '{{ lookup("env", "PROXMOX_USER") }}@{{ proxmox_realm }}'
proxmox_api_password: '{{ lookup("env", "PROXMOX_PASS") }}'
proxmox_api_host: pve.diesel.net
proxmox_node: pve
