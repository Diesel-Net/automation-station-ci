all:
  children:
    
    manager:
      hosts:
        unity.diesel.net:

    drone:
      hosts:
        localhost:

  vars:
    ansible_user: automation
    ansible_python_interpreter: /usr/bin/python3
